<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="com.gadgeon.pro4tech.kotlin">

    <application
            android:name="com.gadgeon.pro4tech.kotlin.utilities.PusherApplication"
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"
            tools:ignore="GoogleAppIndexingWarning">

        <!--<uses-library android:name="androidx.test.ext" />-->

        <activity android:name="com.gadgeon.pro4tech.kotlin.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

    <!--<instrumentation
            android:name="androidx.test.ext.junit"
            android:targetPackage="com.gadgeon.pro4tech.kotlin.data" />-->

</manifest>